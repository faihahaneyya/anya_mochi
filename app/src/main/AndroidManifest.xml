<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.anya_mochi">

    <!-- Permissions Internet -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Permissions Notifikasi (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Permissions Reminder / Alarm -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <!-- Permissions Camera & Storage (Pertemuan 13) -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Anyamochi">

        <!-- BroadcastReceiver untuk Reminder (AlarmManager) -->
        <receiver
            android:name=".utils.ReminderReceiver"
            android:exported="false" />

        <!-- FileProvider untuk Camera Capture -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".saran.SaranFormActivity"
            android:exported="false" />
        <activity
            android:name=".note.NoteFormActivity"
            android:exported="false" />

        <activity
            android:name=".intro.IntroActivity"
            android:exported="true" />
        <activity
            android:name=".VerificationActivity"
            android:exported="false" />
        <activity
            android:name=".RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".SplashScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".BaseActivity"
            android:exported="true" />
        <activity
            android:name=".AuthActivity"
            android:exported="false" />
        <activity
            android:name=".WebViewActivity"
            android:exported="false" />

        <activity
            android:name=".pertemuan_2.MainActivity"
            android:exported="false" />
        <activity
            android:name=".pertemuan_2.PersegiPanjangActivity"
            android:exported="false" />
        <activity
            android:name=".pertemuan_2.TabungActivity"
            android:exported="false" />
        <activity
            android:name=".pertemuan_3.OnboardingActivity"
            android:exported="false" />
        <activity
            android:name=".pertemuan_3.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".pertemuan_3.WelcomeActivity"
            android:exported="false" />
        <activity
            android:name=".pertemuan_4.JobBoardActivity"
            android:exported="false" />
        <activity
            android:name=".pertemuan_4.PortofolioActivity"
            android:exported="false" />
        <activity
            android:name=".pertemuan_13.ThirteenthActivity"
            android:exported="false" />

        <!-- Mengunduh model ML Kit secara otomatis lewat Google Play Services -->
        <meta-data
            android:name="com.google.mlkit.vision.DEPENDENCIES"
            android:value="barcode" />

    </application>

</manifest>